import { NgModule, Component } from '@angular/core';
import { RouterModule, Routes } from '@angular/router';


const MnRoutes: Routes = [
  
  
];

@NgModule({
  imports: [RouterModule.forChild(MnRoutes)],
  exports: [RouterModule]
})
export class MnRoutingModule { }
